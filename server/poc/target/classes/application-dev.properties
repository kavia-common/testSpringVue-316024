# DEV profile (Oracle)
# --------------------
# Derived from application.properties_template.
# IMPORTANT: Do not commit secrets. Replace placeholders via environment or local override.

# Oracle settings
### Remember to configure your ojdbc.properties in your wallet for .jks, spring does not support .sso/.p12
spring.datasource.url=${ORACLE_JDBC_URL:jdbc:oracle:thin:@###TNSNAMES###?TNS_ADMIN=###WALLET_PATH+/###}
spring.datasource.username=${ORACLE_USERNAME:###DB_OWNER###}
spring.datasource.password=${ORACLE_PASSWORD:###DB_PASSWORD###}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Hibernate (keep mapping to VW_USERS)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.Oracle12cDialect

# HikariCP settings
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=5

# logging (dev is noisier by default)
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n
logging.level.org.hibernate.SQL=debug
#logging.level.org.hibernate.type.descriptor.sql=trace
logging.level.=error

# CORS (centralized; see com.upiara.poc.CorsConfiguration)
spring.cors.allowed-origins=
spring.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.cors.allowed-headers=Authorization,Content-Type,Accept,Origin,X-Requested-With
spring.cors.allow-credentials=true
spring.cors.max-age=3600
